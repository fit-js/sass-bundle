# top-most EditorConfig file
root = true

# Unix-style newlines with a newline ending every file
[*]
end_of_line = lf
insert_final_newline = true

#Customizable width tabs for JS, HTML, et al
indent_style = tab

# Set default charset
charset = utf-8

#Trim trailing whitespace
trim_trailing_whitespace = true
